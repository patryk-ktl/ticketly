spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticketly
    username: ticketly
    password: ticketly
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:db/changelog-master.xml
    enabled: true
    contexts: dev
